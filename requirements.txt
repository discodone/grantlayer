# GrantLayer Python runtime dependencies
# These are the non-stdlib packages imported by backend/src/* at runtime.

cryptography>=42.0.0
psycopg2-binary>=2.9.0

# GL-228: FastAPI layer (Phase 1 migration — strangler fig pattern)
fastapi>=0.110.0
uvicorn>=0.27.0

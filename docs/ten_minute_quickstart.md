# Ten Minute Quickstart

Status and readiness caveats: see `README.md`.

This quickstart is local/demo only: no production deployment, no real customer
data, no real secrets, and no shared multi-tenant operation.

```bash
git clone https://github.com/Discodone/grantlayer.git
cd grantlayer
```

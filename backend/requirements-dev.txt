pytest==9.1.0
pytest-xdist>=3.0.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0.0
httpx>=0.27.0
# Used by config-validation tests (e.g. test_gl312 loads a compose/OTEL yaml).
# Declared here so every CI job gets it deterministically instead of relying on
# an ad-hoc `pip install pyyaml` in one job's step.
PyYAML==6.0.2
